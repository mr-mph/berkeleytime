/**
 * Per-course enrollment refresh from classes.berkeley.edu.
 * Flip to `true` to re-enable the class-page refresh button and GraphQL mutation.
 * Disabled at UC Berkeley IT's request.
 */
export const ENROLLMENT_CATALOG_REFRESH_ENABLED = false;
